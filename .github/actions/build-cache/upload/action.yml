name: Upload build tree to cache
description: Publishes a tar.zst + sha256 sidecar as release assets in the cache repo.
inputs:
  tar-path:
    required: true
  checksum-path:
    required: true
  tag-name:
    required: true
  cache-repo:
    description: "owner/repo of the cache repository"
    required: true
  token:
    required: true
runs:
  using: composite
  steps:
    - uses: softprops/action-gh-release@v3
      continue-on-error: true
      with:
        tag_name: ${{ inputs.tag-name }}
        files: |
          ${{ inputs.tar-path }}
          ${{ inputs.checksum-path }}
        make_latest: true
        prerelease: true
        body: ${{ inputs.tag-name }}
        token: ${{ inputs.token }}
        repository: ${{ inputs.cache-repo }}
